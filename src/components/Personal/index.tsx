import React, {FC} from 'react';
import Sidebar from "./Sidebar";

const Area: FC = () => {
    return (
        <div className="area">
            <Sidebar/>
        </div>
    );
};

export default Area;